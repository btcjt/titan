[package]
name = "titan-types"
description = "Core types for the Titan nsite browser"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
hex = "0.4"
